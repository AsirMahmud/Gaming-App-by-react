import { Button } from "@chakra-ui/react";
import { color } from "framer-motion";

const App = () => {
  return (
    <div>
      <Button flex={}>ASIR</Button>
    </div>
  );
};

export default App;
